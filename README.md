# CRUD User Project

## 📌 Giới thiệu
Dự án này là một bài tập thực hành với **JavaScript, HTML, CSS** nhằm xây dựng một module **CRUD User** đơn giản.  
Ứng dụng sử dụng API giả lập từ [JSONPlaceholder](https://jsonplaceholder.typicode.com/users) để thao tác dữ liệu người dùng.

## 🎯 Chức năng
- **GET**: Lấy danh sách user từ API và hiển thị trong bảng (bỏ trường `company` và `address`).
- **POST**: Thêm user mới thông qua form nhập liệu và gửi lên API.
- **Validate**: Kiểm tra dữ liệu đầu vào (email, phone, website, required fields).
- **Async/Await**: Toàn bộ thao tác API sử dụng cú pháp bất đồng bộ hiện đại.
- **UI**: Giao diện đơn giản với bảng danh sách và form thêm user.

## 🛠️ Công nghệ sử dụng
- **HTML5**: Tạo cấu trúc giao diện.
- **CSS3**: Thiết kế bảng và form.
- **JavaScript (ES6+)**: Xử lý logic, gọi API, CRUD.
- **Fetch API**: Gọi API RESTful.
- **JSONPlaceholder**: API giả lập dữ liệu người dùng.

## 🚀 Cách chạy dự án
1. Clone repo:
   ```bash
   git clone https://github.com/Project-DevPlus/CRUD-user.git
